import XCTest
@testable import ios_trusted_device

final class ios_trusted_deviceTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ios_trusted_device().text, "Hello, World!")
    }
}
